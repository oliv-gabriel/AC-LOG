import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({

     button: {
        width: '25%',
        height: 52,
        backgroundColor: '#482a80',
        borderRadius: 10,
        justifyContent: "center",
        alignItems: "center",
    },
    title: {
        color: '#fff',
        fontSize: 16,fontWeight: 'bold',
    }
})

