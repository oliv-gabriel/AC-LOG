import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input:{
        width: '50%',
        height: 40,
        borderRadius: 8,
        borderWidth: 1,
        borderColor: '#482a80',
        fontSize: 16,
    }
});